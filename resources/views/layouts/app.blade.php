
    <!-- Call header -->
    @include('partials.header')

    <!-- Call content -->
    <div class="">
        @yield('content')
    </div>

    <!-- Call footer -->
    @include('partials.footer')
    